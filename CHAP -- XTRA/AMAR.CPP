#include<stdio.h>
#include<conio.h>
int sum=0,1,avg,a[20],i;
void main()
{
clrscr();
printf("enter the no. of entries:");
scanf("%d",%1);
for(i=0;i<=1;i++)
{
printf("\n enter no.%d",i+1);
scanf("%d",%a[i]);
sum=sum+a[i];
}
avg=sum/1;
printf("\n the average of the series of number=%d",avg);
getch();
}